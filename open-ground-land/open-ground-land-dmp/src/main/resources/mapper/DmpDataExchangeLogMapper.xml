<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.openground.land.dmp.mapper.DmpDataExchangeLogMapper">

    <sql id="Base_Column_List">
        id, config_id, task_type, start_time, end_time, duration_seconds, run_status, row_count, error_msg,
        create_by, create_time
    </sql>

    <insert id="insert" parameterType="io.github.openground.land.dmp.entity.DmpDataExchangeLog">
        INSERT INTO DMP_DATA_EXCHANGE_LOG (
            id, config_id, task_type, start_time, end_time, duration_seconds, run_status, row_count, error_msg,
            create_by, create_time
        ) VALUES (
            #{id}, #{configId}, #{taskType}, #{startTime}, #{endTime}, #{durationSeconds}, #{runStatus}, #{rowCount}, #{errorMsg},
            #{createBy}, #{createTime}
        )
    </insert>

    <update id="update" parameterType="io.github.openground.land.dmp.entity.DmpDataExchangeLog">
        UPDATE DMP_DATA_EXCHANGE_LOG
        <set>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="durationSeconds != null">duration_seconds = #{durationSeconds},</if>
            <if test="runStatus != null">run_status = #{runStatus},</if>
            <if test="rowCount != null">row_count = #{rowCount},</if>
            <if test="errorMsg != null">error_msg = #{errorMsg},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" resultType="io.github.openground.land.dmp.entity.DmpDataExchangeLog">
        SELECT <include refid="Base_Column_List"/>
        FROM DMP_DATA_EXCHANGE_LOG
        WHERE id = #{id}
    </select>

    <select id="selectList" resultType="io.github.openground.land.dmp.entity.DmpDataExchangeLog">
        SELECT <include refid="Base_Column_List"/>
        FROM DMP_DATA_EXCHANGE_LOG
        <where>
            <if test="configId != null">AND config_id = #{configId}</if>
            <if test="taskType != null and taskType != ''">AND task_type = #{taskType}</if>
            <if test="runStatus != null and runStatus != ''">AND run_status = #{runStatus}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectCount" resultType="int">
        SELECT COUNT(*) FROM DMP_DATA_EXCHANGE_LOG
        <where>
            <if test="configId != null">AND config_id = #{configId}</if>
            <if test="taskType != null and taskType != ''">AND task_type = #{taskType}</if>
            <if test="runStatus != null and runStatus != ''">AND run_status = #{runStatus}</if>
        </where>
    </select>

</mapper>
