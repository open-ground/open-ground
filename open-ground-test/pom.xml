<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.open-ground</groupId>
        <artifactId>open-ground</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>open-ground-test</artifactId>
    <packaging>jar</packaging>

    <name>Open Ground Test</name>
    <description>Open Ground 测试模块 —— 模拟业务模块使用 open-ground 框架，覆盖全部组件测试</description>

    <properties>
        <!-- 不发布到 Maven 仓库 -->
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <dependencies>
        <!-- ==================== Open Ground 框架（模拟业务模块的依赖方式） ==================== -->
        <!-- starter-all：一键引入全部模块 -->
        <dependency>
            <groupId>io.github.open-ground</groupId>
            <artifactId>open-ground-starter-all</artifactId>
            <version>${revision}</version>
            <type>pom</type>
        </dependency>

        <!-- Lombok（starter 中为 optional，需显式声明） -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- ==================== Open Ground Land ==================== -->
        <dependency>
            <groupId>io.github.open-ground</groupId>
            <artifactId>open-ground-land-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.open-ground</groupId>
            <artifactId>open-ground-land-dispatch</artifactId>
        </dependency>

        <!-- ==================== 测试 ==================== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
